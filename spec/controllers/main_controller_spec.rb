#
#
#

require "spec_helper"

describe MainController do

  context "with no data" #do

#    connections = []
        
#    Connection.stub_chain(:order_by,:paginate).and_return( connections) #should_receive(:order_by).and_return context
 #   context.should_receive(:paginate).and_return connections
    
#    Connection.should_receive(:count).and_return 1
#    
#    subject { visit root_path }
#    assigns(:page).should eq(1)
#    assigns(:pages).should eq(1)
#    it_behaves_like "a valid HTML valid response"
#  end

end
